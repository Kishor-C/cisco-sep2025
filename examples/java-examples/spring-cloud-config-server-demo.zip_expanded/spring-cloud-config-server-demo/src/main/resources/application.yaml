spring:
  application:
    name: spring-cloud-config-server-demo
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Kishor-C/common-config.git
        default-label: master
server:
  port: 8888
